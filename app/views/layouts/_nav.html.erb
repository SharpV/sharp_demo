<%= tabs_tag(:namespace => :site_menus, :open_tabs => {:id => "user_menus", :class => "nav" }, :builder => MenuTabBuilder) do |tab| %>
  <%= tab.home '首页', root_path %>
  <%= tab.contact '好友', contacts_path %>
  <%= tab.note '笔记', notes_path %>
  <%= tab.group '群组', groups_path %>
  <%= tab.event '课程', events_path %>	
  <%= tab.question '问答', questions_path %>
  <%= tab.user '知识库', users_path %>
<% end %>
