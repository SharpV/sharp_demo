<!DOCTYPE html>
<html lang="en">
  <head>
    <%= render 'layouts/head' %>
  </head>
  <body>

    <%= render "layouts/header" %>
    <div class="page container">
      <div class='left'>
        <%= image_tag avatar_url(@group) %>
        <h3><%= @group.name %></h3>
        <p><%= @group.body %></p>
      </div>

      <div class='right'>
        <%= bootstrap_flash %>
        <%= render 'group/nav' %>
        <%= yield %>
      </div>

    </div> <!-- /container -->
    <%= render 'layouts/footer' %>
  </body>
</html>
