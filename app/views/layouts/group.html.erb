<!DOCTYPE html>
<html lang="en">
  <head>
    <%= render 'layouts/head' %>
  </head>
  <body>
    <%= render "layouts/header" %>
    <div id="page" class="container">
      <div class="row">
        <div class='span-l'>
          <div class='avatar'><%= link_to image_tag(avatar_url(@group, 'b')), @group %></div>
          <h4><%= @group.name %></h4>
        </div>

        <div class='span-r'>
          <%= render 'group/nav' %>
          <%= bootstrap_flash %>
          <%= yield %>
        </div>
      </div>
    </div> <!-- /container -->
    <%= render 'layouts/footer' %>
  </body>
</html>
