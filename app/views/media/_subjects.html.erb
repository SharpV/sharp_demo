<ul id="subjects_nav" class="mega-menu">
  <%- Grade.child_grade.each do |grade| %>
    <li>
      <%= link_to grade.name, [grade, :media] %>
      <ul>
        <%- grade.subjects.each do |subject| %>
          <li>
            <%= link_to subject.name, [subject, :media] %>
          </li>
        <% end %>
      </ul>
    </li>
  <% end %>
</ul>
