<%= form_for(@me_school_class) do |f| %>
  <% if @me_school_class.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@me_school_class.errors.count, "error") %> prohibited this me_school_class from being saved:</h2>

      <ul>
      <% @me_school_class.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
