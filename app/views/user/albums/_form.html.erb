<%= simple_form_for @album, url: [:user, current_user, @album], validate: true do |f| %>
  <%= f.input :name, label: '名称' %>
  <%= f.submit value: '创建', class: 'btn btn-info', disable_with: "正在提交..." %>
<% end %>