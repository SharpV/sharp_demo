<h4>
  <%= @current_user.name %>的相册
  <%- if @current_user.id == current_user.id %>
    <%= link_to "创建相册", [:new, :user, current_user, :album], class: 'btn btn-success pull-right', remote: true, data: {type: 'script'} %>
  <% end %>
</h4>