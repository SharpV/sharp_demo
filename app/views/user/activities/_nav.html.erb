<%= tabs_tag(:namespace => :activities_nav, :open_tabs => { :id => "activity_nav", :class => "nav nav-tabs" }, :builder => MenuTabBuilder) do |tab| %>
  <%= tab.home raw("好友动态"), [:activities, :user, current_user, :followings] %>
  <%= tab.post raw("我的动态"), [:user, current_user, :activities] %>
  <%= tab.question raw("我关注的人"), [:user, current_user, :followings] %>
  <%= tab.question raw("关注我的人"), [:user, current_user, :followers] %>
<% end %>
