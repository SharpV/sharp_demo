<%= form_for(@me_activity) do |f| %>
  <% if @me_activity.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@me_activity.errors.count, "error") %> prohibited this me_activity from being saved:</h2>

      <ul>
      <% @me_activity.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
