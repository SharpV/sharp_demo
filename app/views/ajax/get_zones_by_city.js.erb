$("#school-list").empty().append("<option>请选择学校</option>");
<%- if @zones.size > 0 %>
  $("#zone-list").empty().append("<option>请选择区县</option>").append("<%= escape_javascript(options_for_select(@zones.map{|i|[i.name, i.id]})) %>");
<% end %>