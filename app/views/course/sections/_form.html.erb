<%= form_for @section, url: course_course_sections_path(@course), remote: true, validate: true, html: {class: 'form', id: 'section-form', data: {type: "script", method: :post} } do |f| -%> 
  <%= f.text_field :title, :label => '名称' %>
  <%= f.submit '添加章节', :class=>'btn btn-primary', disable_with: "正在提交..." %>
<% end %>

