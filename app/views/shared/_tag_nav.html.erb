<ul class="nav nav-tabs nav-stacked">
  <% @tags.each do |tag| %>
		<li>
			<%= link_to raw("<i class='icon icon-tag'></i> #{tag.name}"), tag_posts_path(tag.name) %></h4>
		</li>
	<% end %>
</ul>