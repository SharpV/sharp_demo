<%= form_for(@me_group) do |f| %>
  <% if @me_group.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@me_group.errors.count, "error") %> prohibited this me_group from being saved:</h2>

      <ul>
      <% @me_group.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
