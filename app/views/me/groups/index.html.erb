<h1>Listing me_groups</h1>

<table>
  <tr>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @me_groups.each do |me_group| %>
  <tr>
    <td><%= link_to 'Show', me_group %></td>
    <td><%= link_to 'Edit', edit_me_group_path(me_group) %></td>
    <td><%= link_to 'Destroy', me_group, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Group', new_me_group_path %>
