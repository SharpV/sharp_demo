<%= form_for(@me_setting) do |f| %>
  <% if @me_setting.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@me_setting.errors.count, "error") %> prohibited this me_setting from being saved:</h2>

      <ul>
      <% @me_setting.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
