<%- if @my_webclasses.size > 0 %>
  <h4>我的班级</h4>
  <ul>
    <%- @my_webclasses.each do |webclass| %>
      <li>  
        <%= link_to image_tag(avatar_url(webclass), placeholder: 'logo.png'), webclass, target: '_blank' %>
      </li>
    <% end %>
  </ul>
<% end %>