<%= tabs_tag(:namespace => :media_nav, :open_tabs => { :id => "post_nav", :class => "nav nav-tabs" }, :builder => MenuTabBuilder) do |tab| %>
  <%= tab.media raw("我的文件"), me_media_path %>
  <%= tab.folder raw("目录管理"), manage_me_folders_path %>
  <%= link_to "添加目录", new_me_folder_path, class: "btn btn-info pull-right span", remote: true, data: {type: "script"} %>
  <%= link_to '添加文件', new_me_medium_path, :class => 'btn btn-success pull-right span' %>
<% end %>
