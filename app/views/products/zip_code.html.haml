.hero-unit
  %h4 Please type in zip code:
  %p
    = form_tag( get_images_products_path, method: "get", class: 'form-horizontal') do
      .control-group
        = label_tag(:zip, "Zip:", class: 'control-label')
        .controls
          =  text_field_tag(:zip)
          = submit_tag("Submit", class: 'btn')
