.content
  %h4= 'Manuals'
  - @product.product_manuals.each do |manual|
    .pull-left= link_to image_tag(manual.cover.url), manual.file.url
  %h4= 'Images'
  - @product.product_images.each do |image|
    .pull-left= image_tag image.image.url