.comment
  = hidden_field :comment, :id, :value => node.send(opts[:node_id]), :id => nil
  .title
    %span.main= node.send opts[:title]
    - if opts[:level] <= opts[:max_levels]
      %span.reply= link_to '回复', '#'
  .content=  raw node.send opts[:content_field]
