<%= simple_form_for @album, url: webclass_webclass_albums_path(@current_webclass), validate: true do |f| %>
  <%= f.input :name, label: '名称' %>
  <%= f.submit value: '创建', class: 'btn btn-info', disable_with: "正在提交..." %>
<% end %>