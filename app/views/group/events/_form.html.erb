<%= form_for(@my_event) do |f| %>
  <% if @my_event.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@my_event.errors.count, "error") %> prohibited this my_event from being saved:</h2>

      <ul>
      <% @my_event.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
